# Extra dependencies for the LOCAL desktop notifier (notifier.py) only.
# Not needed by the Streamlit dashboard or the cloud deploy.
#   .venv\Scripts\python.exe -m pip install -r requirements-notifier.txt
winotify
requests   # ntfy.sh phone push (skipped at runtime if missing)
