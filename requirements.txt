streamlit>=1.31
# starlette cap 2026-08-05: 0.47+ changed GZipResponder's signature
# (requires thread_minimum_size) and streamlit's gzip middleware
# 500-crashes on every request with it installed.
starlette<0.47
pandas>=2.0
numpy>=1.24
requests>=2.31
plotly>=5.18
vaderSentiment>=3.3
tradingview-ta>=3.3
truststore>=0.9
pybit>=5.7
flask>=3.0
gunicorn>=21.2

# 🔮 KRONOS deps REMOVED from the server build 2026-08-05: torch's
# resident memory OOM-crashed the instance into a 500 loop, and its
# build size risked failing deploys outright. Kronos still runs
# locally for research; server-side is opt-in via KRONOS_ENABLED=1.
# To restore: upgrade the instance to ~2GB and uncomment:
# --extra-index-url https://download.pytorch.org/whl/cpu
# torch>=2.0
# einops>=0.8
# huggingface_hub>=0.30
# safetensors>=0.4
# tqdm>=4.66
