# Python
__pycache__/
*.py[cod]
*.pyc

# Virtual environment — never commit (huge, machine-specific)
.venv/
venv/

# Secrets — NEVER commit these
.env
.streamlit/secrets.toml

# Claude Code local files
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db

# Local artifacts
screenshot*.png
*.log

# Notifier state — local, machine-specific
.notifier_seen.json

# Paper trading bot state — your virtual trades, machine-specific
.paper_bot.json

# Live trading bot state — your REAL trades on Bybit, machine-specific
.live_bot.json

# 24/7 Agent bot state — separate paper history for the agent section
.agent_bot.json

# Signal fire log — persisted STRONG+ signal fires for the
# 🔥 RECENT FIRES section (machine-specific)
.signal_fires.json

# Local test scratch files
.symbol_test.py
.watchlist_test.py
.premium_test.py
.plan_test.py
.commit_msg.tmp
